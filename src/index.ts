/**
 * @packageDocumentation
 * A typed Bitbucket Cloud client. This package also ships the `bb` CLI, which is a
 * process rather than a module and is reachable only through its `bin` entry.
 */

export { loadJq } from "./output/jq.js";
export type { JqEngine } from "./output/jq.js";
